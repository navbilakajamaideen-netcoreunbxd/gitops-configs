apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: capacitor
  namespace: flux-system
spec:
  targetNamespace: flux-system
  interval: 1h
  path: "./"
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
